type Breakpoints = {
  xs: string;
  sm: string;
  md: string;
  lg: string;
  xl: string;
};

const breakpoints: Breakpoints = {
  xs: "480px",
  sm: "600px",
  md: "768px",
  lg: "1024px",
  xl: "1280px"
};

export { breakpoints };
export type { Breakpoints };
